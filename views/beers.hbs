<div>
    {{#each beersFromApi}} 
        <div>
            <h4>{{name}}</h4>
            <img src="{{image_url}}" alt="glass of beer">
            <p>{{description}}</p>
            <p>{{tagline}}</p>
        </div>
    {{/each}}
</div>